#include <sys/types.h>
#include <sys/stat.h>
#include <sys/shm.h>
#include <sys/ipc.h>

int main() {
    char * args;
    fgets(args, 3, stdin);
    if(strcmp(args, "-c") == 0){

    }
    if(strcmp(args, "-r") == 0) {

    }
    if(strcmp(args, "-v") == 0) {

    }
}
